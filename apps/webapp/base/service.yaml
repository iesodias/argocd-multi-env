apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: webapp-PLACEHOLDER
  labels:
    app: webapp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: webapp
